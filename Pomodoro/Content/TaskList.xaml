<UserControl x:Class="Pomodoro.Content.TaskList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             >
            
    <Grid>
        <DataGrid  Grid.Row="2" Grid.RowSpan="2" 
                       VerticalAlignment="Top" ItemsSource="{Binding  Tasks}" 
                      AutoGenerateColumns="False" 
                      CanUserResizeRows="False"
                      CanUserResizeColumns="False" SelectedIndex="0" GridLinesVisibility="Horizontal"
                      CanUserAddRows="False" FrozenColumnCount="3" HeadersVisibility="Column" 
                      SelectedItem="{Binding SelectedTask}"
                      
                      >

            <DataGrid.Columns>
                <DataGridTextColumn IsReadOnly="True" Binding="{Binding Path= Title}" ClipboardContentBinding="{x:Null}" Header="Title" Width="160" />
                <DataGridTextColumn IsReadOnly="True" Binding="{Binding Path= Priority}" ClipboardContentBinding="{x:Null}" Header="Priority" Width="80"/>
                <DataGridTextColumn IsReadOnly="True" Binding="{Binding Path= State}" ClipboardContentBinding="{x:Null}" Header="Status" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
